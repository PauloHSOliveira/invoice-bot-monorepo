{
  "name": "backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --filter ./functions build",
    "dev": "firebase emulators:start",
    "serve": "pnpm --filter ./functions build && firebase emulators:start",
    "deploy": "firebase deploy --only functions",
    "type-check": "pnpm --filter ./functions type-check"
  },
  "dependencies": {
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "firebase-tools": "^13.0.0"
  },
  "engines": {
    "node": "22"
  }
}
